
project_id = "fwjfbenicnnprhkmnsmm"

[functions.send-contact-email]
verify_jwt = false

[functions.create-subscription]
verify_jwt = true

[functions.check-subscription]
verify_jwt = true

[functions.customer-portal]
verify_jwt = true

# Stripe calls this endpoint directly, so JWT verification must be disabled.
# Authenticity is enforced via the Stripe webhook signature instead.
[functions.stripe-webhook]
verify_jwt = false

# Auth is enforced inside the function so logged-out users can still load
# designated preview chapters (Genesis 1 / Mere Christianity 1).
[functions.get-challenge]
verify_jwt = false
